# Wallpaper Gallery

一个高雅大气的壁纸展示网站，基于 Vue 3 + Vite 7 构建。

## 特性

- 🖼️ **高清壁纸展示** - 支持 4K 及更高分辨率壁纸
- 🔍 **智能搜索** - 按文件名快速搜索
- 📊 **多种排序** - 按时间、大小、名称排序
- 🏷️ **自动标签** - 根据分辨率自动标记（标清/高清/4K/超清）
- 🌓 **主题切换** - 深色/浅色主题，自动跟随系统
- 📱 **响应式设计** - 完美适配桌面端和移动端
- ✨ **流畅动画** - 精心设计的过渡动画效果
- ⬇️ **一键下载** - 直接下载原图

## 技术栈

- **框架**: Vue 3.5 (Composition API + script setup)
- **构建工具**: Vite 7
- **样式**: Sass
- **部署**: GitHub Pages + GitHub Actions

## 开发

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 生成壁纸数据
npm run generate

# 构建生产版本
npm run build

# 预览构建结果
npm run preview
```

## 部署

项目配置了 GitHub Actions 自动部署：

1. 推送代码到 `main` 分支
2. GitHub Actions 自动构建并部署到 GitHub Pages
3. 访问 `https://<username>.github.io/wallpaper-gallery/`

### 首次部署配置

1. 进入仓库 **Settings** → **Pages**
2. **Source** 选择 **GitHub Actions**
3. 推送代码触发部署

## 项目结构

```
wallpaper-gallery/
├── .github/workflows/    # GitHub Actions 配置
├── public/
│   └── data/            # 壁纸数据 JSON（构建时生成）
├── scripts/
│   └── generate-data.js # 数据生成脚本
├── src/
│   ├── assets/styles/   # 全局样式（Sass）
│   ├── components/      # Vue 组件
│   ├── composables/     # 组合式函数
│   └── utils/           # 工具函数
└── index.html
```

## 数据源

壁纸数据来自 [IT-NuanxinPro/nuanXinProPic](https://github.com/IT-NuanxinPro/nuanXinProPic) 图床仓库。

## License

MIT
